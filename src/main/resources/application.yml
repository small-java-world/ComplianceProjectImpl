spring:
  datasource:
    url: jdbc:mysql://172.27.0.2:3306/compliance_management_system?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: compliance_user
    password: compliance_pass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: true
    out-of-order: false
    clean-disabled: false
    url: jdbc:mysql://172.27.0.2:3306/compliance_management_system?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    user: compliance_user
    password: compliance_pass

minio:
  url: ${MINIO_URL:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minio}
  secret-key: ${MINIO_SECRET_KEY:minio123}
  bucket: ${MINIO_BUCKET:compliance-bucket}

logging:
  level:
    root: INFO
    com.example.project: ${LOG_LEVEL:INFO}
    org.jooq.tools.LoggerListener: ${JOOQ_LOG_LEVEL:INFO} 