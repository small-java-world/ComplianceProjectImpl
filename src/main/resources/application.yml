spring:
  datasource:
    organization:
      url: jdbc:mysql://localhost:3307/organization_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    code-master:
      url: jdbc:mysql://localhost:3307/code_master_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    document:
      url: jdbc:mysql://localhost:3307/document_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    framework:
      url: jdbc:mysql://localhost:3307/framework_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    audit:
      url: jdbc:mysql://localhost:3307/audit_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    training:
      url: jdbc:mysql://localhost:3307/training_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    pool:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1200000

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jooq:
    sql-dialect: MYSQL
  flyway:
    enabled: true
    baseline-on-migrate: true
    out-of-order: true
    validate-on-migrate: false

minio:
  url: ${MINIO_URL:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minio}
  secret-key: ${MINIO_SECRET_KEY:minio123}
  bucket: ${MINIO_BUCKET:compliance-bucket}

logging:
  level:
    root: INFO
    com.example.project: DEBUG
    org.jooq: DEBUG
    org.jooq.tools.LoggerListener: DEBUG 